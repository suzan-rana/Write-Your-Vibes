import { type NextPage } from "next";
import Head from "next/head";
import Link from "next/link";
import { signIn, signOut, useSession } from "next-auth/react";
import Button from "~/components/ui/Button";
import Navbar from "~/components/ui/Navbar";
import { NextPageWithLayout } from "./_app";
import Layout from "~/components/ui/Layout";

const Home: NextPageWithLayout = () => {
  const {  status } = useSession();

  if (status !== "authenticated") {
    return <Link href={"/auth/login"}>Login with email and password</Link>;
  }

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="mx-auto text-center">
        <section className="h-[85vh]">

          <h1 className="pb-8 pt-28 text-3xl font-bold capitalize tracking-wide sm:leading-loose lg:text-6xl">
            Vibe: Next Generation Blogging Tool for Students.
          </h1>
          <p className="mx-auto text-xl text-gray-400 sm:w-[70%] ">
            I&apos;m building a web app with Next.js 13 and open sourcing everything.
            Follow along as we figure this out together.
          </p>
          <div className="mt-8 flex items-center justify-center gap-6">
            <Button>
              <Link href={"/create"}>Get Started</Link>
            </Button>
            <Button variant={"ghost"}>
              <Link href={"/blog"}>Read more</Link>
            </Button>
          </div>
        </section>
        <section>
          <h2 className="mb-6 text-center text-3xl  font-bold">Features</h2>
          <p className="mx-auto mb-6 text-lg text-gray-400 sm:w-[70%] ">
            I&apos;m building a web app with Next.js 13 and open sourcing everything.
            Follow along as we figure this out together.
          </p>
        </section>
      </main>
    </>
  );
};

export default Home;
Home.getLayout = (page: React.ReactElement) => {
  return <Layout>{page}</Layout>;
};
